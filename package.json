{
  "name": "code-ai",
  "version": "1.0.0",
  "description": "telegram bot for code-ai",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./build && tsc",
    "start:dev": "npx nodemon",
    "start": "npm run build && node build/index.js"
  },
  "keywords": [
    "ai",
    "telegram"
  ],
  "author": "oasis-cover-band",
  "license": "ISC",
  "dependencies": {
    "add": "^2.0.6",
    "dotenv": "^16.0.3",
    "firebase": "^9.15.0",
    "openai": "^3.1.0",
    "telegraf": "^4.11.2"
  },
  "devDependencies": {
    "@types/node": "^18.11.17",
    "nodemon": "^2.0.20",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  }
}
